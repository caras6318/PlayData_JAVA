Wordcnt 작성하기
txt파일2종류를 읽어오고 그안에 검색할 단어가 각각 몆개씩있는지 확인하는 프로그램
 
 wordcnt
 worddata
 wordmethod
 wordprint
 
 그냥 일단검색할 단어가있는텍스트열어서 배열에다가 다 때려박고
 그거랑 같은사이즈의 배열(rescnt)만든다음에 
 검색할텍스트 열어서 한줄씩읽어서 검색할단어랑 같으면 
 (검색한 단어의 인덱스 번호하고 같은 인덱스 번호의 rescnt에 숫자 더해줌)
 마지막에 검색할단어 출력하고 rescnt에 들어있는 숫자 출력하고 다음줄 
 
 
 틀린 이유 String.contains();는 포함하고 있는지 아닌지로 TRUE , FALSE로 값이 리턴되기 때문에 
 해당 문자열이 몆번이나 등장하냐를 판단하기 위해서는 한줄이아니라 한문장을 구분자로나눠서 판단했어야 했다.
 아니면 아예 다른 메소드를 사용하던가 
 
ss = BufferedReader.readLine()이걸로 한문장씩 읽어온다음에
ss.split(" ")이런식으로 나눠서 String[] 배열에 추가한다음에 검사면 될꺼같기는 한데 문제는 이런식으로하면
base가 되는 문서가 길어지면 길어질수록 시간소요가 엄청날꺼같단말이지 
  